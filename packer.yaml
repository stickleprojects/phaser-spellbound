# config file for atlas-pack
# see https://github.com/implicit-invocation/pack-atlas#readme

characters:
  images: ./src/images/characters/*.png
  outDir: './build/assets'
  maxwidth: 1024
  maxheight: 1024
  padding: 2
  allowRotation: false
  square: false
  smart: true
  trim: true
